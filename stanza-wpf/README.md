# Stanza

[Stanza 纯文本任务管理格式](https://github.com/Ryougi-Shiki0/Stanza) 的 Windows 桌面编辑器。极简设计，结构化查看与编辑 `.stanza` 文件。

## 构建与运行

```bash
dotnet build
dotnet run --project src/Stanza.App            # 启动
dotnet run --project src/Stanza.App -- TODO.stanza   # 启动并打开文件
dotnet test                                    # 运行解析器测试
```

要求：.NET 8 SDK 或更高版本，Windows。

## 使用

| 操作 | 方式 |
| --- | --- |
| 打开文件 | 拖入窗口 / `Ctrl+O` / 命令行参数；启动时自动恢复上次文件 |
| 切换最近文件 | 悬停左下角区域，点击列表项；`×` 从列表移除 |
| 新建文件 | `Ctrl+Shift+N` |
| 保存 | `Ctrl+S`；输入停止约 1.2 秒后自动保存 |
| 新建任务 | `Ctrl+N` 或底部工具栏按钮（追加到区块末尾） |
| 指定位置新建 | 新建后拖拽调整位置 |
| 选中任务 | 单击（高亮指示条） |
| 编辑任务 | 快速双击展开；`Enter` 展开选中项；`Esc` 或点击空白处收起 |
| 填写属性 | 展开后在主行内联书写：`(A) 优先级`、`YYYY-MM-DD` 截止、`+项目`、`#标签` |
| 排序 | 按住卡片拖拽，实时预览落点 |
| 移动到其他区块 | 拖拽卡片到侧栏区块上松手；或右键菜单选择 |
| 切换区块 | 侧栏点击 / `Ctrl+1~4` |
| 完成任务 | 点击任务前的勾选框（渐变消失动画后进入 DONE） |
| 清空 DONE / DELETE | 底部工具栏「清空」（二次确认后生效） |
| 任务操作 | 右键任务卡片：完成 / 移到 WAIT / 移到 DOING / 废弃 / 恢复 / 彻底删除（按状态显示） |
| 完成 / 废弃 / 恢复 / 彻底删除 | 展开卡片底部的操作按钮 |

## 行为说明

- 解析与写回遵循 Stanza RFC 1.0.0：四状态区块、主行元数据、缩进续行、备注内空行等边界规则全部实现并有测试覆盖。
- 备注为纯文本多行编辑，空内容时以水印提示；折叠卡片只显示一个备注标记，列表保持紧凑。
- 「完成」（勾选框或按钮）会将任务移至 DONE 顶部、移除优先级并在备注末尾追加完成日期（RFC §9）；「废弃」移至 DELETE 顶部；「恢复」移回 DOING 末尾。
- 保存为规范化输出：UTF-8（无 BOM）、LF、区块标题大写、按 DOING / WAIT / DONE / DELETE 顺序、任务间空行分隔。
- DOING / WAIT 始终自动按（优先级 → 截止日期）稳定排序，无需手动操作；同优先级内的相对顺序可用拖拽调整。
- 备注在编辑器中顶格显示，保存时统一为 4 空格缩进。

## 项目结构

```
src/Stanza.Core        解析器 / 写出器 / 文档模型（无 UI 依赖）
src/Stanza.App         WPF 应用（MVVM，零第三方依赖）
tests/Stanza.Core.Tests  RFC §10.3 全部用例 + 往返一致性测试
```
